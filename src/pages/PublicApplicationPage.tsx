/**
 * ATS-style public application page — short URLs (/a/:shortCode, /j/:, /c/:).
 * Reuses PublicFormPage UI via re-export after adapting load/submit for short links.
 */
import PublicFormPage from './PublicFormPage';

export default PublicFormPage;
